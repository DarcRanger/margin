name: Build and Deploy Site

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: docs/package.json

      - name: Install dependencies
        run: |
          cd docs
          npm install

      - name: Build docs
        run: |
          cd docs
          npm run build

      - name: Prepare deploy folder
        run: |
          mkdir deploy
          cp index.html deploy/
          cp -r assets deploy/
          cp -r docs/.vitepress/dist deploy/docs
          if [ -f stats/history.json ]; then
            cp stats/history.json deploy/stats.json
            mkdir -p deploy/stats
            cp stats/history.json deploy/stats/history.json
          fi
          touch deploy/.nojekyll

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./deploy
